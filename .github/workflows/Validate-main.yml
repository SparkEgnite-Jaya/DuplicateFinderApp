name: Validate PR to Main

on:
  pull_request:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Salesforce CLI
        run: npm install @salesforce/cli --global

      - name: Authenticate Packaging Org
        run: |
          echo "${{ secrets.PACKAGING_ORG_SECRET }}" > authfile.txt
          sf org login sfdx-url --sfdx-url-file authfile.txt --alias production_org

      - name: Validate Deployment
        run: |
          sf project deploy validate \
            --target-org production_org \
            --source-dir force-app \
            --test-level RunLocalTests \
            --coverage-formatters text \
            --wait 60